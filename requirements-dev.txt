# Development and test dependencies.
# Install the runtime set first:  pip install -r requirements.txt
-r requirements.txt

pytest==9.1.1
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0

# linting. E/W layout, F real problems (unused imports, undefined names), I import order.
# configured in pyproject.toml
ruff==0.16.2
